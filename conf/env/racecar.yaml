# @package _global_
env_name: SafetyPointFadingHard1Vision-v0
vll_epochs_dyn: 200
num_steps: 1000000 # Override default agent steps
red_dim: 300
train_vll: true
# Humanoid needs stochastic noise for balance
eval_deterministic: false
# Reduce batch size for Vision
vll_finetune_batch_size: 256
